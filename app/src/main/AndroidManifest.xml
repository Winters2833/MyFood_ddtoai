<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFood_ddtoai"
        tools:targetApi="31">

        <!-- Màn hình đăng nhập là launcher -->
        <activity
            android:name=".LoginActivity_ddtoai"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Màn hình chính sau khi đăng nhập -->
        <activity android:name=".HomeActivity_ddtoai" />

        <!-- Màn hình đăng ký tài khoản -->
        <activity android:name=".RegisterActivity_ddtoai" />

        <activity android:name=".FoodListActivity_ddtoai" />

        <activity android:name=".FoodDetailActivity_ddtoai" />

    </application>

</manifest>
